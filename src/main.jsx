import React from 'react'
import ReactDOM from 'react-dom/client'
import SeatPlaner from './SeatPlanerDittmann.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <SeatPlaner />
    </React.StrictMode>,
)
